---
context: phase
phase: 05-organization
task: 5
total_tasks: 5
status: paused_uat
last_updated: 2026-05-18T17:30:00+03:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| SSL on music.youtube.com | spotDL and ytmusicapi both fail with SSL CERTIFICATE_VERIFY_FAILED on this machine (antivirus/proxy). Only yt-dlp (--no-check-certificate) and requests to open.spotify.com work. | blocking | Do NOT add any library that makes direct HTTPS to music.youtube.com or new domains. Use ONLY yt-dlp ytsearch: for YouTube operations. |
| Songs absent from YouTube | Some Spotify songs don't exist on YouTube at all. No matching algorithm can fix this. ytsearch will return a wrong song silently. | blocking | Surface failure clearly in UI — show row as FAILED with message, let user provide direct YouTube URL. |

<current_state>
Phase 5 (Organization) code is complete, 80/80 tests GREEN, repo cleaned for public launch.

Active problem: Spotify URL → YouTube matching. `_find_best_yt_match` uses quoted-title yt-dlp search + title substring matching. Works when the song exists on YouTube. Fails silently when it doesn't — ytsearch fallback downloads wrong song.

Root cause discovered this session: the tested song does not exist on YouTube — only a different artist has a song with the same title. No search-based fix is possible for this case.

The correct fix: when no confident match found, mark row FAILED instead of silently downloading wrong content.
</current_state>

<completed_work>

- Phase 5 Plans 01-04: all complete (commit 86499fe)
- Code review: 7 issues fixed (CR-01/02, WR-01..05)
- UAT bugs fixed: UI lock, Start button blocked, shutil.move Windows, UI unlock edge case
- 80 automated tests GREEN (commit 3cf747c)
- Repo cleaned: no artist/song names in code comments, test fixtures, or commit messages
- Tried and ruled out: duration matching (too fragile), spotDL (SSL), ytmusicapi (SSL), Deezer API (removed)
- Implemented: `_find_best_yt_match` — quoted title search + full title substring match (commit 029e8f1)
- Debugged: confirmed some songs are simply absent from YouTube
</completed_work>

<remaining_work>

- Fix: when `_find_best_yt_match` returns None and ytsearch also gives wrong result,
  mark row as FAILED with a clear message ("Song not found on YouTube")
  and optionally allow user to paste a direct YouTube URL as override
- Phase 6 (iBroadcast Upload) — discuss + plan + execute
</remaining_work>

<decisions_made>

- Do NOT use ytmusicapi or spotDL — both fail SSL on this machine
- Only yt-dlp (--no-check-certificate) and requests to open.spotify.com are reliable here
- _find_best_yt_match: quoted title ytsearch + full title substring match (not word-by-word)
- No artist/song names in code, tests, or commit history — public repo
- When no YouTube match: surface failure in UI, don't silently download wrong content
</decisions_made>

<blockers>
- Some Spotify songs don't exist on YouTube — needs UX decision (fail row vs manual URL input)
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full structured state with all decisions
2. `tunebridge.py` — `_find_best_yt_match()` and `_download_worker()` Spotify branch

## Infrastructure State
- Python 3.13, PySide6, yt-dlp, librosa, spotdl (installed but SSL-broken) installed
- 80/80 tests passing: `python -m pytest tests/ -q --tb=short -p no:warnings`
- Working tree: `.planning/phases/05-organization/05-HUMAN-UAT.md` modified (not committed)
- No running background processes

<context>
The Spotify matching issue has a clear root cause: some songs simply aren't on YouTube. The matching algorithm is not at fault. The fix is a UX change — fail loudly instead of silently downloading wrong content. After that, Phase 6 (iBroadcast Upload) can begin.
</context>

<next_action>
In `_download_worker`, when `_find_best_yt_match` returns None: instead of silently calling ytsearch (which may return wrong song), emit `FAILED` status with message "Not found on YouTube — paste a direct YT URL". Optionally: add a "retry with URL" flow where the user can provide a direct YouTube URL for the row.
</next_action>
