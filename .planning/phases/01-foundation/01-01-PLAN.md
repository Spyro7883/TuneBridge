---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/__init__.py
  - tests/test_tunebridge.py
  - pytest.ini
autonomous: true
requirements:
  - PROC-01
  - PROC-02
  - GUI-01

must_haves:
  truths:
    - "pytest discovers and runs all 7 test cases without import errors"
    - "All tests fail (RED) because tunebridge.py does not yet exist"
    - "Test file covers SongStatus enum, BatchTable API, worker count formula, and dark theme colors"
  artifacts:
    - path: "tests/__init__.py"
      provides: "makes tests/ a package"
    - path: "tests/test_tunebridge.py"
      provides: "7 test cases covering GUI-01, PROC-01, PROC-02"
    - path: "pytest.ini"
      provides: "pytest configuration pointing testpaths at tests/"
  key_links:
    - from: "tests/test_tunebridge.py"
      to: "tunebridge.py"
      via: "import TuneBridgeApp, BatchTable, SongStatus"
      pattern: "from tunebridge import"
---

<objective>
Create the test scaffold for Phase 1 before any implementation exists. All 7 tests must be importable and runnable — they will fail RED because tunebridge.py does not exist yet. This wave establishes the verification contract that Plan 02 must satisfy.

Purpose: Establish TDD gate. Plan 02 is done only when all 7 tests pass GREEN.
Output: tests/test_tunebridge.py, tests/__init__.py, pytest.ini
</objective>

<execution_context>
@C:/Users/Eusebiu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eusebiu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pytest config and test scaffold with 7 test cases</name>
  <files>pytest.ini, tests/__init__.py, tests/test_tunebridge.py</files>

  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md — sections "Validation Architecture" and "Pattern 3: SongStatus State Machine" and "Pattern 2: BatchTable Component" for exact test contracts
    - retune_app.py lines 205-218 — to understand the existing Style setup pattern being replicated
  </read_first>

  <behavior>
    - test_worker_count_formula: assert min(1,4)==1, min(2,4)==2, min(4,4)==4, min(7,4)==4, min(0,4)==0 — pure arithmetic, no import needed
    - test_status_enum_values: import SongStatus; assert list of .value strings equals ["Queued", "Fetching metadata", "Downloading", "Retuning", "Awaiting folder", "Saving", "Uploading", "Done ✓", "Failed ✗"] in that exact order
    - test_app_initializes: instantiate TuneBridgeApp(); call root.withdraw(); assert isinstance(app, tk.Tk); call root.destroy() in teardown
    - test_dark_theme_colors: after TuneBridgeApp() + withdraw(), assert style.lookup("TLabel","background") == "#121212" and style.lookup("Title.TLabel","foreground") == "#1DB954"
    - test_batch_table_columns: after TuneBridgeApp() + withdraw(), assert app.table.tree["columns"] == ("title", "type", "status")
    - test_batch_table_api: add_row("https://example.com", "Test Song", "[Spotify]") returns a non-empty string iid; update_row_status(iid, "Downloading") does not raise; tree.set(iid, "status") == "Downloading"
    - test_status_tags: after update_row_status(iid, "Done ✓") assert tree.item(iid, "tags") == ("done",); after update_row_status(iid, "Failed ✗") assert tree.item(iid, "tags") == ("failed",); after update_row_status(iid, "Awaiting folder") assert tree.item(iid, "tags") == ("waiting",)
  </behavior>

  <action>
1. Create pytest.ini at project root with content:
   ```ini
   [pytest]
   testpaths = tests
   addopts = -x -q
   ```

2. Create tests/__init__.py as empty file.

3. Create tests/test_tunebridge.py with these exact requirements:
   - File header: `# -*- coding: utf-8 -*-` (required for unicode ✓/✗ on Windows)
   - Add sys.stdout UTF-8 wrapper at module level for safe printing:
     `import io, sys; sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")`
   - Import block: `import pytest, tkinter as tk; from tkinter import ttk`
   - All tkinter-touching tests must use a fixture `app_root` that:
     a) Calls `app = TuneBridgeApp()`
     b) Calls `app.withdraw()` immediately (no phantom window)
     c) Yields `app`
     d) Calls `app.destroy()` in teardown (via try/finally)
   - Import statement for tunebridge: `from tunebridge import TuneBridgeApp, BatchTable, SongStatus`
     This import will fail RED until Plan 02 creates tunebridge.py — that is expected and correct.

4. Implement all 7 test functions as specified in the behavior block above.
   - test_worker_count_formula: no fixture needed, pure assert statements
   - test_status_enum_values: no fixture, pure enum check
   - test_app_initializes through test_status_tags: all use app_root fixture
   - For test_dark_theme_colors: get style via `ttk.Style(app_root)` (returns existing style instance on clam theme)
   - For test_status_tags: call app_root.table.add_row(...) then update_row_status in sequence — use app_root.update() between calls if needed to flush pending events
  </action>

  <verify>
    <automated>cd C:/Users/Eusebiu/Downloads/FrontendProjects/RetuneProj && python -m pytest tests/test_tunebridge.py --collect-only -q 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - pytest.ini exists and contains `testpaths = tests`
    - tests/__init__.py exists (may be empty)
    - tests/test_tunebridge.py exists and contains exactly 7 test functions named: test_worker_count_formula, test_status_enum_values, test_app_initializes, test_dark_theme_colors, test_batch_table_columns, test_batch_table_api, test_status_tags
    - Running `python -m pytest tests/test_tunebridge.py --collect-only -q` collects exactly 7 items (no collection errors)
    - Running `python -m pytest tests/test_tunebridge.py -x -q` produces errors/failures (not collection errors) — specifically ImportError on `from tunebridge import` because tunebridge.py does not yet exist; this is the expected RED state
    - File starts with `# -*- coding: utf-8 -*-`
    - File contains `from tunebridge import TuneBridgeApp, BatchTable, SongStatus`
    - File contains `app.withdraw()` inside the fixture
    - File contains `app.destroy()` inside the fixture teardown
  </acceptance_criteria>

  <done>pytest --collect-only reports 7 items; running tests fails with ImportError (tunebridge not found) — RED state confirmed, scaffold ready for Plan 02</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → tunebridge module | Test imports production module; no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | test file | accept | Test file is developer-controlled; no user input in Phase 1 |
</threat_model>

<verification>
Run after task completes:
```
python -m pytest tests/test_tunebridge.py --collect-only -q
```
Expected: 7 test items collected, no syntax errors, ImportError on run (RED state).
</verification>

<success_criteria>
- pytest.ini, tests/__init__.py, tests/test_tunebridge.py all exist
- 7 test cases collected by pytest without syntax/collection errors
- Tests fail RED (ImportError) because tunebridge.py does not yet exist — this is correct
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the summary template.
</output>
